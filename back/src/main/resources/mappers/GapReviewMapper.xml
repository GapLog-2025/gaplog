<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaplog.reviews.mapper.GapReviewMapper">

    <insert id="insertReview" parameterType="GapReview">
        INSERT INTO gap_reviews(user_id, title, major, content, grade, gap_period)
        VALUES (#{userId}, #{title}, #{major}, #{content}, #{grade}, #{gapPeriod})
    </insert>

    <select id="findById" parameterType="long" resultType="GapReview">
        SELECT * FROM gap_reviews WHERE gap_reviews_id = #{id}
    </select>

    <select id="findAll" resultType="GapReview">
        SELECT * FROM gap_reviews ORDER BY created_at DESC
    </select>

    <delete id="deleteById" parameterType="long">
        DELETE FROM gap_reviews WHERE gap_reviews_id = #{id} AND user_id = #{userId}
    </delete>
</mapper>